import React from 'react';
import PollCard from "@/entities/poll-card/ui/PollCard.jsx";

const Polls = () => {
    return (
        <section>
            <h2>Опросы</h2>
            <PollCard/>
        </section>
    );
};

export default Polls;
